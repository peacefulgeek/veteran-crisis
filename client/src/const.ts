export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// De-Manus: the public site has no OAuth and no users.
// This stub is kept only so any leftover imports still type-check.
export const getLoginUrl = () => "/";
